import express from "express";
import verifyToken from "../middlewares/verifyToken";
// const History = require("../schema/history.scheme");
const historyRoutes = express.Router();

export default historyRoutes;
